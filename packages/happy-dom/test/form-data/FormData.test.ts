// Import { URLSearchParams } from 'url';
// Import FormData from '../../src/form-data/FormData';

describe('FormData', () => {
	describe('constructor', () => {
		it('Supports being sent as a parameter to URLSearchParams.', () => {
			// TODO: Add support for this?
		});
	});
});
